<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram3.user.UserMapper">
    <insert id="insUser" useGeneratedKeys="true" keyProperty="iuser">
        insert into t_user set
        uid= #{uid}, upw= #{upw}, nm= #{nm}, pic= #{pic}
    </insert>
    <select id="selUser">
        select iuser, uid, upw, nm, pic, created_at as createdAt,
        updated_at as updatedAt from t_user
        <where>
            <if test="uid!=null and uid!=''">
                uid= #{uid}
            </if>
        </where>
    </select>
    <insert id="insFollow">
        insert into t_user_follow
        set from_iuser= #{fromIuser},
        to_iuser= #{toIuser}
    </insert>
    <delete id="delFollow">
        delete from t_user_follow
        where from_iuser= #{fromIuser} and to_iuser= #{toIuser}
    </delete>
</mapper>